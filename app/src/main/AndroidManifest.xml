<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teststock">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_coccinelle_trefle_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_coccinelle_trefle_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestStock">
        <activity android:name=".controllers.ItemDetailActivity" />
        <activity android:name=".controllers.MainActivity" />
        <activity android:name=".controllers.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
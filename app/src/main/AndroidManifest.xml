<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teststock">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_coccinelle_trefle_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestStock">
        <activity android:name=".controllers.activities.ImportActivity" />
        <activity android:name=".controllers.activities.TestActivity" />
        <activity android:name=".controllers.activities.DictionaryActivity" />
        <activity android:name=".controllers.activities.EditItemActivity" />
        <activity android:name=".controllers.activities.ItemDetailActivity" />
        <activity android:name=".controllers.activities.MainActivity" />
        <activity android:name=".controllers.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>